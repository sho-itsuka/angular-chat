import { CommentDatePipe } from './commentDatePipe';

describe('CommentDatePipe', () => {
  it('create an instance', () => {
    const pipe = new CommentDatePipe();
    expect(pipe).toBeTruthy();
  });
});
